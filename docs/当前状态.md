# 当前状态
更新时间：2026-04-02

## 基本信息

- 项目名称：加油记录 PWA
- 当前版本：`V3.6.5`
- 当前工作分支：`0331`
- 线上发布分支：`0331`
- 最新提交：以仓库当前 `HEAD` 为准
- 仓库地址：`git@github.com:zhangwuji0630/jiageyouba-v34.git`

## 最近一次发布

- 发布版本：`V3.6.5`
- 发布形态：GitHub Pages 静态站点 + PWA
- 发布入口：`https://zhangwuji0630.github.io/jiageyouba-v34/`

## 已完成事项

- 看板快返时静态占位值露出问题已完成代码修复，并补上 hydration gate。
- 全局数字显示已统一去掉千分号。
- 趋势页、历史页、设置页残留静态占位问题已完成代码修复。
- 五个页面的运行时随机文案模块已统一补上 `data-runtime-copy-group` gate，避免先露出静态文案。
- 站内页面切换已补上通用离场压屏机制，覆盖 `看板/记录/历史/设置 -> 趋势` 等共享导航路径。
- 页面标题、设置页可见版本标识、`manifest.webmanifest`、`app.js`、`supabase/setup.sql` 与 `service-worker.js` 已同步升级到 `V3.6.5`。
- 用户新增的长期协作规则已同步到 `docs/协作规则.md`、本地 `zhangwuji-project-guard` skill 与仓库备份 skill。
- `docs/` 连续性文档体系与仓库内 skill 备份目录已落地。

## 已完成验证

- `node --check app.js` 通过。
- 代码审计确认 `app.js` 中站内 JS 跳转已统一收口到 `navigateTo(...)`。
- 使用本地静态服务 + Playwright Headless 浏览器验证：
  - `index.html -> stats.html`
  - `add.html -> stats.html`
  - `logs.html -> stats.html`
  - `settings.html -> stats.html`
  - 以上四条路径均已确认在 URL 变化前先挂上 `app-page-leaving`，且离场遮罩伪元素已生效。
- 使用禁用 JS 的浏览器检查确认：
  - `index.html`、`add.html`、`stats.html`、`logs.html`、`settings.html` 的 `data-runtime-copy-group` 初始状态均为 `visibility: hidden`。
- 版本同步检查已确认：
  - 页面标题、设置页可见版本标识、`manifest.webmanifest`、`app.js`、`supabase/setup.sql`、`service-worker.js` 均已切到 `V3.6.5` / `v365`。

## 当前发布状态

- 本轮目标是提交、推送并发布 `V3.6.5`。
- 推送到 `0331` 后由 GitHub Pages 承载站点更新。

## 已知问题

- 手机端若仍命中旧缓存，可能短时间看到旧 UI，需要刷新或重装 PWA 后再核验新代码。
- 新会话或新设备默认不具备历史对话记忆，仍需依赖仓库文档与个人 skill 恢复上下文。

## 待验证项

- 真机安装态 PWA 是否完全消除站内切页旧页闪帧。
- 发布后缓存是否稳定命中最新前端资源与 `jiageyouba-v365-cache-v1`。
- 后续任务执行时是否持续遵守“先回复，再执行；先审计同类点，再集中修复”的协作规则。
