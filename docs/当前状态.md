# 当前状态
更新时间：2026-04-02

## 基本信息

- 项目名称：加油记录 PWA
- 当前版本：`V3.6.7`
- 当前工作分支：`0331`
- 线上发布分支：`0331`
- 最新已提交：以仓库当前 `HEAD` 为准
- 仓库地址：`git@github.com:zhangwuji0630/jiageyouba-v34.git`

## 最近一次发布

- 发布版本：`V3.6.7`
- 发布形态：GitHub Pages 静态站点 + PWA
- 发布入口：`https://zhangwuji0630.github.io/jiageyouba-v34/`

## 本次发布包含

- 以本地 `V3.6.3` 稳定基线为基础发布，不重新带回 `V3.6.5`、`V3.6.6` 的切页离场效果改动。
- 修复历史页“累计支出”金额区与页脚装饰区首屏先露出占位内容的问题。
- 修复历史页在没有当前车辆时只剩空白区域、不渲染空态卡片的问题。
- 修复设置页车辆卡、备份恢复卡、云同步状态块首屏先露出占位内容的问题。
- 同步升级页面标题、设置页可见版本标识、`manifest.webmanifest`、`app.js`、`supabase/setup.sql` 与 `service-worker.js` 到 `V3.6.7`。

## 已完成验证

- `node --check app.js` 通过。
- 本地静态服务已确认 `logs.html` 可正常返回 `200`。
- 代码审计确认本轮首屏占位修复落在 `app.css`、`app.js`、`logs.html`、`settings.html`。
- 版本同步检查已确认：
  - 页面标题、设置页可见版本标识、`manifest.webmanifest`、`app.js`、`supabase/setup.sql`、`service-worker.js` 已切到 `V3.6.7` / `v367`。

## 当前发布状态

- `V3.6.7` 发布内容已完成仓库内同步。
- 推送到 `0331` 后由 GitHub Pages 承载站点更新。

## 已知问题

- 手机端若仍命中旧缓存，可能短时间继续看到旧版首屏内容，需要刷新或重开 PWA 后再核验。
- 新会话或新设备默认不具备历史对话记忆，仍需依赖仓库文档与个人 skill 恢复上下文。

## 待验证项

- 历史页金额区与页脚装饰区在真机上是否已不再先露出占位内容。
- 设置页车辆卡、备份恢复卡、云同步状态块在真机上是否已不再先露出占位内容。
- 发布后客户端是否稳定切到 `V3.6.7` 与 `jiageyouba-v367-cache-v1`。
