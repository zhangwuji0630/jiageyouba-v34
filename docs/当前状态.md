# 当前状态
更新时间：2026-04-02

## 基本信息

- 项目名称：加油记录 PWA
- 当前版本：`V3.6.6`
- 当前工作分支：`0331`
- 线上发布分支：`0331`
- 最新已提交：以仓库当前 `HEAD` 为准
- 仓库地址：`git@github.com:zhangwuji0630/jiageyouba-v34.git`

## 最近一次发布

- 发布版本：`V3.6.6`
- 发布形态：GitHub Pages 静态站点 + PWA
- 发布入口：`https://zhangwuji0630.github.io/jiageyouba-v34/`

## 本次发布包含

- 移除页面切换时的全屏黑色离场遮罩。
- 将页面离场视觉改为轻微模糊、轻微淡出和轻微缩放，避免全站黑闪。
- 修复趋势页“本月支出总额”金额行只隐藏数字、不隐藏 `¥` 的问题。
- 修复历史页“累计支出”金额行只隐藏数字、不隐藏 `CNY` 的问题。
- 同步升级页面标题、设置页可见版本标识、`manifest.webmanifest`、`app.js`、`supabase/setup.sql` 与 `service-worker.js` 到 `V3.6.6`。

## 已完成验证

- `node --check app.js` 通过。
- 代码审计确认本轮功能改动只涉及 `app.css`、`stats.html`、`logs.html`。
- 本地静态服务 + Playwright Headless 已确认：
  - `index.html -> stats.html` 离场帧中 `body::after` 为 `none`。
  - 离场截图不再是整屏黑屏。
- 禁用 JS 的浏览器检查已确认：
  - `stats.html` 金额行整块为 `visibility: hidden`。
  - `logs.html` 金额行整块为 `visibility: hidden`。
- 版本同步检查已确认：
  - 页面标题、设置页可见版本标识、`manifest.webmanifest`、`app.js`、`supabase/setup.sql`、`service-worker.js` 已切到 `V3.6.6` / `v366`。

## 当前发布状态

- `V3.6.6` 发布内容已完成仓库内同步。
- 推送到 `0331` 后由 GitHub Pages 承载站点更新。

## 已知问题

- 手机端若仍命中旧缓存，可能短时间继续看到旧版切页效果，需要刷新或重开 PWA 后再核验。
- 新会话或新设备默认不具备历史对话记忆，仍需依赖仓库文档与个人 skill 恢复上下文。

## 待验证项

- 真机安装态 PWA 是否完全消除“黑闪”的主观观感。
- 趋势页与历史页金额英雄区在真机上是否已不再先露出静态币种/单位。
- 发布后客户端是否稳定切到 `V3.6.6` 与 `jiageyouba-v366-cache-v1`。
