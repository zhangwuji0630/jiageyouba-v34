# 最新交接
更新时间：2026-04-02

## 当前目标

已完成 `V3.6.6` 代码与文档同步，当前重点是确认发布后：

- 页面切换不再黑闪
- 趋势页与历史页金额英雄区不再先露出静态币种/单位

## 当前版本

- `V3.6.6`

## 最新已提交

- 以仓库当前 `HEAD` 为准

## 本轮已完成

- 已移除 `body.app-page-leaving::after` 全屏黑色遮罩。
- 已将离场视觉改为轻微模糊、轻微淡出和轻微缩放。
- 已给趋势页“本月支出总额”金额行整行补上 `data-page-hydration-gate`。
- 已给历史页“累计支出”金额行整行补上 `data-page-hydration-gate`。
- 已同步页面标题、设置页可见版本标识、`manifest.webmanifest`、`app.js`、`supabase/setup.sql`、`service-worker.js` 到 `V3.6.6`。
- 已补 `change-logs/2026-04-02-navigation-black-flash-and-hero-amount-gate.md`。
- 已补 `change-logs/2026-04-02-v3.6.6-release.md`。

## 已完成验证

- 本地静态服务 + Playwright Headless 已确认：
  - `index.html -> stats.html` 离场帧里 `body::after` 为 `none`
  - 离场截图不再是整屏黑屏
- 禁用 JS 的浏览器检查已确认：
  - `stats.html` 金额行整块隐藏
  - `logs.html` 金额行整块隐藏
- 版本字符串搜索已确认 `V3.6.6` / `v366` 已覆盖本轮发布所需落点。

## 未完成

- 还没有做真机或安装态 PWA 的人工肉眼回归。

## 下一步先做什么

- 发布后先在真机或安装态 PWA 上复测页面切换和趋势/历史金额英雄区。
- 如果用户再提同类问题，先从共享离场逻辑与英雄区整块 gate 审计，不要再做单点补丁。

## 不要乱动的地方

- 不要回退 `V3.6.6` 版本同步。
- 不要顺手改无关页面布局、文案或业务逻辑。
- 不要把这轮修复和额外功能改动混在一起。

## 重点复查项

- 页面切换时是否还会被主观感知为黑闪。
- 趋势页“本月支出总额”金额行是否还会先露出 `¥` 或 `0.00`。
- 历史页“累计支出”金额行是否还会先露出 `CNY` 或 `0.00`。
- 发布后客户端是否稳定切到 `V3.6.6` 与 `jiageyouba-v366-cache-v1`。
