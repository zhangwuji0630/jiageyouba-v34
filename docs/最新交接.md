# 最新交接

更新时间：2026-04-02

## 当前目标

建立并固定“4+2 连续性体系”，并补齐仓库内 skill 备份，确保后续换设备或新会话可无缝接管项目。

## 当前版本

- `V3.6.4`

## 最新提交

- `8a9f9c0`（分支：`0331`）

## 已完成

- 看板页快速切换动效与闪帧问题已修复并推送。
- 设置页“录入偏好”按钮深色模式刺眼问题已修复并推送。
- 看板快返静态占位值露出问题已完成代码修复。
- 全局数字格式已统一关闭千分号。
- `V3.6.4` 版本号、页面可见版本标识、缓存键、日志与连续性文档已同步。
- 趋势页、历史页、设置页残留静态占位问题已完成本地代码修复。
- 五个页面的运行时随机文案首帧闪出问题已完成本地代码修复。
- 用户新增长期协作偏好已写入仓库规则与个人 skill。
- 新增并落地连续性文档目录 `docs/`。
- 明确后续必须中文记录与范围控制规则。
- 个人协作 skill `zhangwuji-project-guard` 已创建并通过有效性校验。
- 个人协作 skill 已同步备份到仓库，可在新设备直接恢复。

## 未完成

- 尚缺一轮浏览器内人工回归，重点确认看板快返与趋势/历史/设置首屏都不再露出静态占位，且共享数字去千分号后无误伤。

## 下一步先做什么

- 在浏览器中从记录页、趋势页、设置页快速切回看板，确认指标文本只在 hydration 后显示。
- 抽查趋势页、历史页、设置页里的金额/里程数字，确认千分号已按预期移除。
- 打开趋势页、历史页、设置页冷启动与页间切换首屏，确认不再露出演示值、示例卡或错误登录态。
- 发布后在手机端刷新或重装 PWA，确认 `V3.6.4` 缓存已生效。
- 冷启动历史页并观察累计支出下方文案模块，确认不再先闪出旧句子。
- 后续任何新问题先用简洁中文回复判断，再开始执行；若疑似同类问题，先做同类审计再集中处理。
- 读取 `docs/协作规则.md`，按规则执行下一批需求。
- 新任务完成后同步更新 `change-logs/`、`docs/当前状态.md`、`docs/最新交接.md`。
- 若涉及发布，额外校验版本号与缓存键是否同步。
- 若本地 skill 有更新，同步回写 `codex-skills-backup/zhangwuji-project-guard/`。
- 新设备接手时，先从旧设备或仓库备份恢复 `zhangwuji-project-guard` 再开工。

## 不要乱动的地方

- 非本次需求范围内的页面样式与业务逻辑。
- 未经确认的资源替换（尤其是看板背景图与视觉资产）。
- 未验证即推送的高风险改动。

## 重点复查项

- 快速切页时看板是否仍出现首帧闪旧内容。
- 快速切回看板时是否还会露出静态占位值或错误单位。
- 去掉千分号后，共享数字格式在各页面是否仍保持小数位正确。
- 趋势页、历史页、设置页是否仍会露出静态演示值或示例记录卡。
- 页脚随机文案与看板故事文案是否仍会露出静态文本再切换。
- 深色模式下设置页偏好按钮的对比度与可读性。
- 发布后手机端是否命中最新缓存版本。
- 执行侧是否持续遵守“先回复、后执行”的新协作规则。
