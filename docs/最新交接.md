# 最新交接
更新时间：2026-04-02

## 当前目标

已完成 `V3.6.5` 发布前代码与文档同步，当前应继续保持“共享导航离场压屏 + 运行时文案整块 gate”这组修复，不要回退成单点补丁。

## 当前版本

- `V3.6.5`

## 最新提交

- 以仓库当前 `HEAD` 为准

## 本轮已完成

- 运行时随机文案模块已在五个页面统一补上 `data-runtime-copy-group`。
- `setText()` / `markRuntimeCopyReady()` 已支持整块运行时文案模块 ready 标记。
- 站内导航已统一接入 `navigateTo(...)` 与 `bindPageNavigationGuards()`。
- 离场压屏已补上 `body.app-page-leaving::after` 全屏遮罩。
- 页面标题、设置页可见版本标识、`manifest.webmanifest`、`app.js`、`supabase/setup.sql`、`service-worker.js` 已同步升级到 `V3.6.5`。
- 已补 `change-logs/2026-04-02-navigation-leave-guard-and-runtime-copy-audit.md`。
- 已补 `change-logs/2026-04-02-v3.6.5-release.md`。

## 已完成验证

- `node --check app.js` 通过。
- 代码审计确认共享站内跳转已统一经由 `navigateTo(...)`。
- Playwright 最小浏览器回归已确认：
  - `看板 -> 趋势`
  - `记录 -> 趋势`
  - `历史 -> 趋势`
  - `设置 -> 趋势`
  - 以上四条路径均先出现 `app-page-leaving`，再发生 URL 切换。
- 禁用 JS 的浏览器检查已确认五个页面运行时文案组初始均隐藏。
- 版本字符串搜索已确认 `V3.6.5` / `v365` 已覆盖本轮发布所需落点。

## 未完成

- 还没有做真机或安装态 PWA 的人工肉眼回归。

## 下一步先做什么

- 发布后在真机或安装态 PWA 上复测 `看板/记录/历史/设置 -> 趋势` 的快速切页。
- 如果用户再提同类首屏闪露问题，先从共享导航与共享 gate 审计，不要再做单点修补。

## 不要乱动的地方

- 不要回退共享导航离场机制。
- 不要移除 `data-runtime-copy-group` 与 ready gate。
- 不要顺手改无关布局、文案、图像资源或业务逻辑。

## 重点复查项

- `看板/记录/历史/设置 -> 趋势` 快速切页时，是否还会看见旧页一帧。
- 历史页累计支出下方文案模块是否还会先闪出静态句子。
- 发布后客户端是否稳定切到 `V3.6.5` 与 `jiageyouba-v365-cache-v1`。
